Fix issue #19: Favicon is 121 KB — replace base64 PNG wrapper with true vector SVG.

## Problem
`src/app/layout.tsx` references a favicon that is a 121 KB "SVG" wrapping a 420×420 base64 PNG. This loads on every page visit with none of SVG's scalability benefit.

## Task
1. Read the current favicon at `src/app/icon.svg` (or wherever it's referenced in `src/app/layout.tsx`)
2. Create a proper vector SVG favicon that represents the site (a simple "B" lettermark or abstract shape — keep it minimal, ~2-5 KB). Use the site's ink color (`#141310`) so it works on both light and dark backgrounds.
3. If a `.ico` or PNG fallback is needed for older browsers, generate a small one (32×32)
4. Update `src/app/layout.tsx` metadata to reference the new favicon
5. Remove or replace the old oversized file

## Constraints
- The favicon must work on both light and dark backgrounds
- Keep it simple and minimal — a lettermark or geometric shape
- Max file size target: under 5 KB for the SVG
- Do NOT change any other layout or component code
- Build + lint + typecheck must pass
